<div class="min-h-screen bg-gradient-to-b from-purple-400 via-pink-300 to-yellow-300">
  <!-- Hero Section -->
  <div class="container mx-auto px-4 py-12">
    <div class="text-center mb-12" x-data="{ show: false }" x-init="setTimeout(() => show = true, 100)">
      <h1
        class="text-6xl md:text-7xl font-black text-white mb-4 transform transition-all duration-1000"
        :class="show ? 'translate-y-0 opacity-100' : '-translate-y-10 opacity-0'">
        🎮 Game Arcade 🎮
      </h1>
      <p
        class="text-2xl md:text-3xl text-white font-bold mb-6 transform transition-all duration-1000 delay-200"
        :class="show ? 'translate-y-0 opacity-100' : '-translate-y-10 opacity-0'">
        Play, Learn, and Earn Points!
      </p>
      <div
        class="inline-block bg-white rounded-full px-8 py-4 shadow-2xl transform transition-all duration-1000 delay-300"
        :class="show ? 'scale-100 opacity-100' : 'scale-50 opacity-0'">
        <p class="text-lg font-bold text-gray-700">
          🌟 Earn up to <span class="text-yellow-600 text-2xl">100 points</span> per game!
        </p>
      </div>
    </div>

    <!-- Games Grid -->
    <div class="grid sm:grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
      <%= render partial: 'game_card', locals: {
        subject: 'Spelling',
        emoji: '⚡',
        color: 'from-blue-400 to-blue-600',
        image_path: 'pokemon_poster.jpg',
        name: 'Pokemon Spellcheck',
        description: 'Test your spelling with your favorite Pokemon!',
        path: pokemon_index_path
      } %>

      <%= render partial: 'game_card', locals: {
        subject: 'Math',
        emoji: '🔢',
        color: 'from-green-400 to-green-600',
        image_path: 'math_blackboard.jpg',
        name: "Multiplication",
        description: 'Master multiplication and become a math wizard!',
        path: game_new_simple_math_round_path(game_id: Game.simple_math.id, operator: 'multiplication')
      } %>

      <%= render partial: 'game_card', locals: {
        subject: 'Logic',
        emoji: '🧩',
        color: 'from-purple-400 to-purple-600',
        image_path: 'collections.png',
        name: "Collections",
        description: 'Organize and categorize items to win!',
        path: game_new_collections_round_path(Game.collections.id)
      } %>

      <%= render partial: 'game_card', locals: {
        subject: 'Trivia',
        emoji: '🧠',
        color: 'from-pink-400 to-pink-600',
        image_path: 'trivia.png',
        name: "Trivia Challenge",
        description: 'Show off your knowledge of everything!',
        path: game_trivia_path(game_id: Game.trivia.id)
      } %>
    </div>

    <!-- Motivational Footer -->
    <div class="text-center mt-16 mb-8">
      <div class="inline-block bg-white bg-opacity-90 rounded-2xl px-12 py-8 shadow-2xl">
        <p class="text-3xl font-bold text-gray-800 mb-2">🏆 Keep Learning! 🏆</p>
        <p class="text-lg text-gray-600">The more you play, the smarter you become!</p>
      </div>
    </div>
  </div>
</div>

<style>
@keyframes float {
  0%, 100% {
    transform: translateY(0px);
  }
  50% {
    transform: translateY(-10px);
  }
}

@keyframes pulse-glow {
  0%, 100% {
    box-shadow: 0 0 20px rgba(255, 215, 0, 0.4);
  }
  50% {
    box-shadow: 0 0 40px rgba(255, 215, 0, 0.8);
  }
}

.animate-float {
  animation: float 3s ease-in-out infinite;
}

.animate-pulse-glow {
  animation: pulse-glow 2s ease-in-out infinite;
}
</style>
